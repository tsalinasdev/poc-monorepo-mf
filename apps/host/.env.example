# Module Federation — entry of each remote.
# Dev: the remote's vite dev server. Prod: the bucket/CDN URL that serves its build.
VITE_REMOTE_POKEMON_ENTRY=http://localhost:5174/remoteEntry.js
VITE_REMOTE_DRAGONBALL_ENTRY=http://localhost:5175/remoteEntry.js
