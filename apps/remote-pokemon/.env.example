# API
VITE_API_BASE_URL=https://pokeapi.co/api/v2

# Module Federation — absolute public path of THIS build.
# Dev/standalone: leave empty (falls back to http://localhost:5174/).
# Prod: the bucket/CDN URL that serves this remote, e.g.
# VITE_PUBLIC_PATH=https://storage.googleapis.com/my-bucket/remote-pokemon/
VITE_PUBLIC_PATH=
